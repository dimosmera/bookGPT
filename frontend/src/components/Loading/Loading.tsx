import styles from "./Loading.module.css";

const Loading = () => {
  return (
    <div className={styles.container}>
      Hold on... this might take up to 12 seconds
    </div>
  );
};

export default Loading;
